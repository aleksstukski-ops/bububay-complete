# Progress - 2026-04-20

## What was completed
- BubuBay web was brought back online on port 8001 and mapped back through the existing Cloudflare tunnel
- frontend build now serves correctly from the backend
- auth hardened:
  - JWT secret removed from hardcoded fallback path
  - bcrypt password hashing added for app logins
  - admin password reset and login verified
- forgot-password flow implemented:
  - forgot-password endpoint
  - reset-password endpoint
  - login page button and reset UI
  - debug reset tokens disabled afterward
- account credentials moved away from base64 toward Fernet encryption for future writes
- docs bootstrap work started

## Current known state
- project is for owner use only
- web is live again
- account credential encryption is in code
- no stable auto-start for backend has been implemented yet
- no verified real marketplace credential save test is documented yet

## Highest-priority next steps
1. Commit the repo documentation bootstrap
2. Validate account credential encryption by saving a real password and inspecting stored value
3. Design product master and channel adapter interface
4. Add integration research docs for priority channels
5. Add stable service management so port 8001 survives restarts cleanly

## Risks
- backend still depends on a local long-running process and can drop off, causing 502
- current product is still a partial prototype, not yet a durable operating system
- some target channels will require partner access or have partial capability only
